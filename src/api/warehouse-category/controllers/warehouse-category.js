'use strict';

/**
 * warehouse-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::warehouse-category.warehouse-category');
